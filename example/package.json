{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && webpack --config webpack.config.js"
  },
  "author": "lotp (http://blog.lotp.xyz/)",
  "license": "ISC",
  "devDependencies": {
    "webpack": "2"
  }
}
